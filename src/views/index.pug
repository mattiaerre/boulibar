extends layout

block content
  section.hero
    .hero-body
      .content
        p.has-text-centered
          a.button.has-text-weight-bold.is-large.is-primary(href="/quote")= copy['quote']
        h1.is-uppercase.title= copy[path]
        p= copy['lorem-ipsum']
        blockquote= page.info
