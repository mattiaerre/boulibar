extends layout

block content
  if page.sections.includes('quote')
    include quote
  if page.sections.includes('upload')
    include upload
  .section
    .container
      .content
        h1.is-uppercase.title= copy[page.key]
        p= copy['lorem-ipsum']
        blockquote= page.info
