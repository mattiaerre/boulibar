extends ../layout
include ../components/pdf

block content
  .section
    .container
      .content
        h1= copy[page.key]
  .section
    .container
      .content
        figure
          img(src="https://bulma.io/images/placeholders/256x256.png")
        p= copy['payment-content-1']
        h2= copy['check']
        p= copy['payment-content-2']
          address= copy['company-address']
        h2= copy['credit-card-authorization-form']
        p= copy['payment-content-4']
        +pdf("/documents/MOP Credit card authorization form .pdf", "Credit Card Authorization Form")
        h2= copy['credit-and-debit-card']
        p= copy['payment-content-3']
  .section
    .container
      .content
        #root
        script(src= appPath)
  .section
    .container
      .content
        #paypal-button-container
        script(src="javascripts/paypal.js")
