extends ../layout

block content
  .section.spacer
  .section
    .container
      .content
        h1.is-serif.title= copy[page.key]
        p= copy['about-content-1']
        p= copy['about-content-2']
        p= copy['about-content-3']
